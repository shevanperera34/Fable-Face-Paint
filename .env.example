# Canonical site URL for sitemap, robots, metadata, and JSON-LD (production).
SITE_URL=https://www.fablefacepaint.com

# Optional: subdirectory deploy (must match next.config if used).
# NEXT_PUBLIC_BASE_PATH=
